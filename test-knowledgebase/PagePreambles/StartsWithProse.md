Buckets decide how long data is kept and how it is billed, and every record lands in exactly one
of them.

## Default buckets

Every table has a default bucket, which is where records land when no rule assigns them elsewhere.

> **Chunk check**
>
> - Path — `docs/page-preambles/starts-with-prose/default-buckets.md`
> - Name — `Starts With Prose: Default buckets`
> - Description — the paragraph above
> - Covers — an ordinary section on a page with no title heading

> **Chunk check for the preamble**
>
> - Path — `docs/page-preambles/starts-with-prose/index.md`
> - Name — `Starts With Prose`
> - Description — the paragraph at the top of this file
> - Covers — a preamble that does not open with a heading. It cannot be a title and an
>   introduction, so it is written out rather than folded into the page summary, and the page
>   title comes from the file name
