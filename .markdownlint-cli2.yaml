ignores:
  - "**/node_modules/**"
  # Knowledge base chunks are generated slices of portal pages, not prose authored here.
  - "docs/**"
  # Neither are the portal documents they are split from.
  - "sources/**"

config:
  MD013: false
